#!/bin/bash
#This shebang is for synax only. Do not make executable!

borg_export () {
    echo borg_export $1 $2
    export $1=$2;
}

borg_addpath () {
    ${BORG_SCRIPT_INPATH} $1 && return
    echo borg_addpath $1
    export PATH=$PATH:$1
}
