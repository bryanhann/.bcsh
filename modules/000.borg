#!/bin/bash
#This shebang is for synax only. Do not make executable!

_export () { export $1=$2; }

_export BORG_MYBIN          ${HOME}/.local/var/myborg/mybin
_export BORG_SCRIPT_INPATH  ${BORG}/notbin/inpath.py

mkdir -p ${BORG_MYBIN}
