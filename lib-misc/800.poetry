# Install poetry to the external folder $HOME/.poetry and activate it.

[ -d ${HOME}/.poetry ] && __note__ echo poetry already installed
[ -d ${HOME}/.poetry ] || curl -sSL https://raw.githubusercontent.com/python-poetry/poetry/master/get-poetry.py | python -
__once__ __run__ source ${HOME}/.poetry/env
